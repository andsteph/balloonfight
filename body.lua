body = {

    new = function(self, x, y, width, height)
        local o = {
            x = x,
            y = y,
            width = width,
            height = height
        }
        return o
    end

}